---
"vite-mcp": minor
---

feat::: new changes and minor fixes

- Component route visualization with active route ↔ component mapping ✌️
- Live, interactive component tree for supported frameworks (React, Vue, etc.)
- Cache Storage explorer for inspecting cached assets and responses
- IndexedDB explorer with record/table browsing and querying
- better adapters update, perf changes, transform module, capture console control, adapter config and more.
- Console and log filtering for improved signal-to-noise during debugging
- Performance metrics including Core Web Vitals and page load timings
