---
"vite-mcp": patch
---

Enhanced README with framework support instructions for React Router, Remix, TanStack Start, and other frameworks. Added TypeScript compatibility for virtual modules with new type declarations (`vite-mcp-env.d.ts` and `virtual-mcp.d.ts`). Improved build configuration to include additional type definitions. Refactored browser bridge for better clarity and error handling. Updated all adapters with improved error handling and type safety.
